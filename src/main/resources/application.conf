mongodb {
  #TODO: use typesafe config to read all environment variables
  database = "sdkman"
  database = ${?MONGODB_DATABASE}
  host = "ds.sdkman.io"
  host = ${?MONGODB_HOST}
  port = "27017"
  port = ${?MONGODB_PORT}
  username = "sdkman"
  username = ${?MONGODB_USERNAME}
  password = "24SAWqYt8UEcfL7fkJvRhyMFqA"
  password = ${?MONGODB_PASSWORD}
}

server {
  port = 8080
  port = ${?PORT}
  host = "0.0.0.0"
  host = ${?HOST}
}
