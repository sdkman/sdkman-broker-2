tasks:
  package:
    action: !io.sdkman.kotlintoolchain.plugins.pkg.packageApplication
      jar: ${module.jar}
      outputJar: ${module.rootDir}/build/libs/${module.name}.jar

commands:
  - name: package
    performedBy: package
